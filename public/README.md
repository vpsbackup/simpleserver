# public static files

Only files in this directory are served by `static_dir` (default `./public`).

Put public pages here, for example:

- `agent.html` → `/agent.html`

Do not point `static_dir` at the repository root, or source/config files may be exposed.
